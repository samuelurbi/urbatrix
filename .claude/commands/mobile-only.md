Apply the following CSS/layout change ONLY inside `@media (max-width: 767px)` blocks. Do NOT touch any styles outside that media query — desktop must remain exactly as it is.

Change to apply: $ARGUMENTS

After making the change, confirm:
1. Which classes you modified
2. That you did NOT change any styles outside `@media (max-width: 767px)`
