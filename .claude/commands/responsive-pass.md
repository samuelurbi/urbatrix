Audit the current state of the page at these three breakpoints: 390px, 768px, and 1024px.

For each breakpoint identify:
- Overflow or horizontal scroll
- Text clipping or truncation
- Spacing/padding that looks inconsistent with the design rhythm
- Elements that are misaligned or break the grid
- Any section that looks visually broken or unfinished

Report as a concise list per breakpoint: class name + issue + suggested fix in one line each. Do not fix anything yet — only report.
