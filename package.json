{
  "name": "urbatrix-web",
  "version": "1.0.0",
  "private": true,
  "description": "Web pública de Urbatrix (Eleventy + HTML/CSS/JS).",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
